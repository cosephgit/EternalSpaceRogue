using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// GridManager
// stores the terrain grid
// has references to each grid space and which pawn is occupying that space
// stores pathing data and does A* algo checks between two points

public class GridManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
